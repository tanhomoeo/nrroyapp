
rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {

    // DEVELOPMENT/TESTING RULE: Allows all reads and writes.
    // WARNING: This rule is insecure and should NOT be used for a live production application
    // with real user data. Secure your rules before going live!
    match /{document=**} {
      allow read, write: if true;
    }

    // --- PRODUCTION RULE EXAMPLES (Commented out) ---
    // You'll need to customize these based on your app's specific needs.

    // Example: Allow only authenticated users to read and write their own user profile data
    // match /users/{userId} {
    //   allow read, update, delete: if request.auth != null && request.auth.uid == userId;
    //   allow create: if request.auth != null;
    // }

    // Example: Allow authenticated users to read 'patients'
    //          Allow specific admin users (via custom claims) to create/update/delete patients
    // match /patients/{patientId} {
    //   allow read: if request.auth != null;
    //   allow create, update, delete: if request.auth != null && request.auth.token.admin == true;
    // }
    // match /patients/{patientId}/{subcollection}/{document=**} { // For subcollections like visits, prescriptions
    //   allow read: if request.auth != null;
    //   allow create, update, delete: if request.auth != null && request.auth.token.admin == true;
    // }

    // Example: Allow authenticated users to read 'visits', 'prescriptions', 'paymentSlips'
    //          related to a patient they can access (more complex, might involve checking patient access first)
    // For simplicity, you might start by allowing any authenticated user to read these if your app logic permits,
    // and restrict writes to specific roles or conditions.

    // match /visits/{visitId} {
    //   allow read: if request.auth != null;
    //   allow create, update, delete: if request.auth != null; // Or more specific rules
    // }
    // match /prescriptions/{prescriptionId} {
    //   allow read: if request.auth != null;
    //   allow create, update, delete: if request.auth != null; // Or more specific rules
    // }
    // match /paymentSlips/{slipId} {
    //   allow read: if request.auth != null;
    //   allow create, update, delete: if request.auth != null; // Or more specific rules
    // }


    // Secure your 'settings/clinic' document
    // Example: Allow authenticated users to read, but only admins to write.
    // match /settings/clinic {
    //   allow read: if request.auth != null;
    //   allow write: if request.auth != null && request.auth.token.admin == true;
    // }
  }
}
