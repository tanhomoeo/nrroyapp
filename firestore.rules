
rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {

    // DEVELOPMENT/TESTING RULE: Allows all reads and writes.
    // WARNING: THIS IS NOT SECURE FOR PRODUCTION.
    // You MUST secure these rules before deploying to a live production environment.
    match /{document=**} {
      allow read, write: if true;
    }

    // --- EXAMPLES OF SECURE RULES (Commented out) ---
    // You will need to adapt these to your specific application needs.

    // Example 1: Allow only authenticated users to read and write their own data
    // Assumes you have a 'users' collection where document IDs are user UIDs.
    // match /users/{userId}/{document=**} {
    //   allow read, write: if request.auth != null && request.auth.uid == userId;
    // }

    // Example 2: Allow authenticated users to read 'patients'
    // but only specific users (e.g., admins with a custom claim) to write.
    // match /patients/{patientId} {
    //   allow read: if request.auth != null;
    //   // For write operations (create, update, delete), require authentication AND an admin custom claim.
    //   // You would set this custom claim (e.g., admin: true) for your admin users via Firebase Admin SDK.
    //   allow create, update, delete: if request.auth != null && request.auth.token.admin == true;
    // }
    // match /visits/{visitId} {
    //   allow read: if request.auth != null;
    //   allow create, update, delete: if request.auth != null && request.auth.token.admin == true;
    // }
    // match /prescriptions/{prescriptionId} {
    //   allow read: if request.auth != null;
    //   allow create, update, delete: if request.auth != null && request.auth.token.admin == true;
    // }
    // match /paymentSlips/{slipId} {
    //   allow read: if request.auth != null;
    //   allow create, update, delete: if request.auth != null && request.auth.token.admin == true;
    // }

    // Example 3: Secure your 'settings/clinic' document
    // Allow authenticated users to read, but only admins to write.
    // match /settings/clinic {
    //   allow read: if request.auth != null;
    //   allow write: if request.auth != null && request.auth.token.admin == true;
    // }
  }
}
