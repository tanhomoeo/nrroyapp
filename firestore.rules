
rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {

    // DEFAULT DEVELOPMENT RULE: Allow all reads and writes.
    // WARNING: THIS IS INSECURE FOR PRODUCTION.
    // You MUST secure these rules before deploying to a live environment.
    match /{document=**} {
      allow read, write: if true;
    }

    // Example of more secure rules (TODO: Implement these based on your app's needs)
    /*
    // Allow only authenticated users to read/write their own patient data
    match /patients/{patientId} {
      allow read, write: if request.auth != null && request.auth.uid == resource.data.ownerUid; 
                           // Assuming you add an 'ownerUid' field to your patient documents
    }

    // Allow authenticated users to read/write their visits
    match /visits/{visitId} {
      allow read, write: if request.auth != null; // Further refine: only owner of linked patient
    }

    // Allow authenticated users to read/write their prescriptions
    match /prescriptions/{prescriptionId} {
      allow read, write: if request.auth != null; // Further refine
    }

    // Allow authenticated users to read/write their payment slips
    match /paymentSlips/{slipId} {
      allow read, write: if request.auth != null; // Further refine
    }

    // Clinic settings might be readable by all authenticated users,
    // but only writable by an admin role (requires custom claims or specific UIDs)
    match /settings/clinic {
      allow read: if request.auth != null;
      allow write: if request.auth != null && request.auth.token.admin == true; // Example admin check
    }
    */
  }
}
