rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Allow read and write access for all documents for testing
    // TODO: Secure these rules before deploying to a live production environment.
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
