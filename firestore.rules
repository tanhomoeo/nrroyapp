
rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {

    // DEVELOPMENT/TESTING RULE: Allows all reads and writes.
    // WARNING: Secure these rules before deploying to a live production environment!
    match /{document=**} {
      allow read, write: if true;
    }

    // --- PRODUCTION EXAMPLES (Commented out) ---
    // Make sure to tailor these rules to your specific application needs.

    // Example: Allow only authenticated users to read and write their own data in a 'users' collection
    // match /users/{userId}/{document=**} {
    //   allow read, write: if request.auth != null && request.auth.uid == userId;
    // }

    // Example: Allow authenticated users to read 'patients' but only specific users (e.g., admins) to write
    // match /patients/{patientId} {
    //   allow read: if request.auth != null;
    //   allow create, update, delete: if request.auth != null && request.auth.token.admin == true; // Requires custom claims
    // }
    // match /visits/{visitId} {
    //   allow read: if request.auth != null;
    //   allow create, update, delete: if request.auth != null && request.auth.token.admin == true;
    // }
    // match /prescriptions/{prescriptionId} {
    //   allow read: if request.auth != null;
    //   allow create, update, delete: if request.auth != null && request.auth.token.admin == true;
    // }
    // match /paymentSlips/{slipId} {
    //   allow read: if request.auth != null;
    //   allow create, update, delete: if request.auth != null && request.auth.token.admin == true;
    // }

    // Secure your 'settings/clinic' document
    // match /settings/clinic {
    //   allow read: if request.auth != null; // Or allow all reads if it's public clinic info
    //   allow write: if request.auth != null && request.auth.token.admin == true; // Only admins can write
    // }
  }
}
