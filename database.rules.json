
{
  "rules": {
    // Warning: These rules grant open access to your Realtime Database.
    // This is suitable for initial development and testing ONLY.
    // For production, you MUST implement more restrictive security rules.
    ".read": true,
    ".write": true
  }
}
